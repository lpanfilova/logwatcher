version: "3.9"

services:
  test-backend:
    build: ./backend_ingest_test
    container_name: test-backend
    ports:
      - "8000:8000"
  demo-app:
    build:
      context: ./demo-app
    container_name: demo-app
    environment:
      PORT: 8888
      LOG_LEVEL: info
    ports:
      - "8888:8888"
  agent:
    build: ./agent
    container_name: log_forwarding_agent
    environment:
      BACKEND_URL: "http://test-backend:8000/api/ingest"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - demo-app
      - test-backend